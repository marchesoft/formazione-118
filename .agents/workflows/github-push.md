---
description: Push changes to GitHub with a commit message
---

1. Stage all changes
// turbo
`git add .`

2. Commit changes
`git commit -m "Auto-update from Antigravity"`

3. Push to GitHub
// turbo
`git push origin main`
